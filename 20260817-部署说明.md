# ImageFlow 部署说明

## 直接部署到 Vercel

这是一个不需要构建步骤的静态站点。将仓库导入 Vercel 后，使用默认的静态项目设置即可：

- Framework Preset：选择 `Other` 或保持自动检测。
- Build Command：留空。
- Output Directory：留空，使用仓库根目录。
- 入口文件：`index.html`。

## URL 参数

页面支持以下查询参数：

- `baseurl`、`base_url` 或 `baseUrl`：OpenAI 兼容接口的根地址，可以带或不带 `/v1`。
- `apikey`、`api_key` 或 `apiKey`：接口的 Bearer API Key。
- `model` 或 `model_name`：模型名称，默认值是 `gpt-image-2`。

示例：

```text
https://your-project.vercel.app/?baseurl=https%3A%2F%2Fgpt222.solov.cc&apikey=sk-example&model=gpt-image-2
```

页面启动后会把配置放入当前标签页的 `sessionStorage`，并从地址栏历史中移除 API Key。API Key 不会写入 `localStorage`；但 URL 参数在首屏请求阶段可能被浏览器、代理或托管平台日志记录，正式环境更推荐打开页面后通过连接设置手动填写。

## Sub2API 自定义页面

Sub2API 的自定义菜单页使用 iframe 加载外部 URL。当前实现会保留自定义 URL 中已有的查询参数，并额外追加以下嵌入参数：

```text
user_id、token、theme、lang、ui_mode、src_host、src_url
```

因此可以在 Sub2API 的自定义菜单 URL 中填写上面的 ImageFlow URL。例如：

```text
https://your-project.vercel.app/?model=gpt-image-2
```

嵌入模式下页面会按以下顺序完成自动连接：

1. 使用 Sub2API 注入的 `token` 请求 `GET /api/v1/auth/me`，确认当前用户仍处于登录状态。
2. 使用同一个会话请求 `GET /api/v1/keys?page=1&page_size=100&search=image&status=active`。
3. 精确匹配名称为 `image` 且状态为 `active` 的 API Key，并将其作为图片接口的 Bearer Key。

当前主线 Sub2API 使用 `/api/v1/keys`；页面同时兼容旧版可能存在的 `/api/v1/api-keys`。如果同名 Key 不存在、已停用或会话失效，页面会保留手动连接入口。Sub2API 的 `token` 只是面板会话令牌，不能直接当作图片接口 Bearer Key 使用。

自动发现的 Key 只保存在当前页面内存中，不写入 `sessionStorage`，避免同一浏览器标签切换 Sub2API 用户后复用旧 Key。手动填写的图片 Key 仍按原逻辑保存到当前标签页的 `sessionStorage`。

### CORS 配置

ImageFlow 部署在独立域名时，Sub2API 必须允许该页面来源，否则浏览器会拦截对 `/api/v1/auth/me` 和 `/api/v1/keys` 的跨域请求。将 ImageFlow 的实际域名加入 Sub2API 配置：

```yaml
cors:
  allowed_origins:
    - https://your-project.vercel.app
  allow_credentials: true
```

不要把 `token` 或图片 API Key 写进自定义菜单 URL。Sub2API 会自动追加 `token`、`user_id`、`src_host` 等嵌入参数；当前页面只从这些参数中读取会话上下文。

## 接口要求

页面调用以下 OpenAI 兼容接口：

- `GET /v1/models`：验证标准 OpenAI 兼容连接。
- `POST /v1/images/jobs`：生图或编辑任务入队（服务支持时优先使用）。
- `POST /v1/images/generations`：队列接口不支持时的标准生图降级路径。
- `POST /v1/images/edits`：队列接口不支持时的标准图片编辑降级路径。
- `GET /v1/images/jobs/{job_id}`：轮询任务状态并读取 `images[].b64_json`。
- `GET /v1/images/jobs?page=1&page_size=30`：刷新任务队列。

不同 OpenAI 兼容网关支持的扩展接口并不一致。如果 `/v1/images/jobs` 返回 `Invalid URL`、`404` 或 `405`，页面会自动改用标准同步图片接口；这种情况下任务仍会显示在本地队列和 IndexedDB 画廊中，但不会出现在服务商的远程队列里。

编辑场景选择“自动适配源图”时，页面会读取第一张上传图片的真实像素宽高和比例，将最长边限制在 2048 像素并按 16 像素步长规范化，最终把计算出的 `宽x高` 显式写入 `size` 参数。这样不会因为省略 `size` 而让上游按 `1024x1024` 默认值处理；多图编辑以第一张图片作为输出画布参考。

编辑任务使用 `multipart/form-data`，重复提交 `image` 字段；生图任务使用 JSON。接口服务器必须允许部署域名的浏览器跨域请求（CORS），并允许 `Authorization` 请求头。

## 本地数据

生成成功的图片会以 Base64 数据保存在当前浏览器的 IndexedDB 中，画廊不会依赖 Vercel 函数或远端数据库。清理浏览器站点数据会同时清空本地画廊。
